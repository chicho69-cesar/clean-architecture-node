version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: mystore
      MYSQL_USER: mysql-user
      MYSQL_PASSWORD: 123456
    volumes:
      - ./mysql:/var/lib/mysql
    ports:
      - 3306:3306
