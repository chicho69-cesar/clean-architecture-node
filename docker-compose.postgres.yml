version: '3.8'

services:
  postgres-db:
    image: postgres:16.2
    restart: always
    environment:
      POSTGRES_USER: postgres-user
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: mystore
    volumes:
      - ./postgres:/var/lib/postgresql/data
    ports:
      - 5432:5432
